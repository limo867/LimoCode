# Coding Agent 分轮开发计划

本文档将 `coding-agent-assignment.md` 的要求拆分为可逐轮完成、验证和复盘的开发任务。后续实现以本计划为工作指引：每轮完成可运行的阶段性版本后，再进入下一轮；若发现设计缺口，应更新本计划和 README。项目会持续迭代，当前不进行最终验收。

## 总体原则

- 不使用 LangChain、LlamaIndex、OpenAI Agents SDK、AutoGen、CrewAI 等 Agent 框架或 SDK。
- 模型 API 仅负责生成文本和原生 Tool Calling；文件操作、命令执行、工具调度、历史管理、循环控制和错误处理均由本项目实现。
- 所有本地操作必须受工作区边界、安全策略、超时和输出长度限制保护。
- 每轮均补充最小必要测试，并保持 README 中的运行说明可复现。
- CLI、Web 前端和桌面 GUI 是同一 Agent 核心的不同入口，不在界面层重复实现工具、历史或循环逻辑。
- 展示层优先呈现真实运行状态、工具调用和结果，不将现成 Agent 产品包装为本项目界面。

## 当前状态

已完成全部计划轮次：前六轮核心能力、第七轮应用服务与事件模型、第八轮本地 Web API/SSE、第九轮 Web 前端、第十轮 tkinter GUI、第十一轮 CLI/日志/文档、第十二轮端到端演示材料。当前通过 19 项自动化测试；后续继续根据真实模型运行和界面反馈迭代。

## 展示形态与技术方向

项目将同时提供以下入口，并共享 `coding_agent` 中的核心能力：

```text
CLI ------------------------------+
                                  |
Web 前端 <-> Web API <------------+-> Agent 核心 -> 本地工具 -> 工作区
                                  |
桌面 GUI -------------------------+
```

- **CLI**：用于调试、自动化测试和无图形环境下运行。
- **Web 前端**：作为主要展示界面。用户可输入任务、选择工作区、实时查看消息、工具调用、文件变更和命令结果。
- **桌面 GUI**：作为本地独立演示入口。建议使用 Python 标准库 `tkinter`，避免在早期为桌面界面增加大量依赖；其功能聚焦于提交任务、查看过程和打开工作区。
- **Web API**：由项目自行实现，封装 Agent 的任务创建、运行状态、事件流和历史读取。后端框架可以后续按实现成本选择轻量 Web 框架或标准库 HTTP 服务；这不是 Agent 框架，且不得把工具执行交给云端服务。

初期优先完成最小可用的 Web 界面，再增加桌面 GUI。两者均通过同一应用服务调用 Agent，不直接操作文件或执行命令。

## 第一轮：基础工程骨架

### 目标

建立不依赖真实模型 API 的最小项目结构，定义各模块职责，并使 CLI 可以离线启动。

### 工作内容

1. 建立 `coding_agent` 包与 CLI 入口 `main.py`。
2. 在 `config.py` 中集中管理工作区、模型名称、API 地址、最大轮数、命令超时和输出上限等配置。
3. 在 `workspace.py` 中实现相对路径解析及工作区越界拒绝。
4. 在 `tools.py` 中定义工具对象、工具 schema、工具注册表和统一错误返回格式。
5. 实现 `list_files`、`read_file`、`write_file`、`run_command` 四个本地工具的初版接口。
6. 在 `agent.py` 中定义模型客户端协议、消息历史容器、最大轮数控制和离线 `DemoModel`。
7. 添加 `.env.example`、`.gitignore` 和 README 的基础运行说明。

### 验收条件

- `python main.py "任意任务"` 可以启动并返回离线演示结果。
- 文件读写操作无法访问工作区外的路径。
- 基础工具测试可通过。

## 第二轮：接入 OpenAI 兼容模型 API

### 目标

实现不依赖第三方 Agent SDK 的原生 HTTP 模型客户端，并支持 OpenAI 兼容 Chat Completions Tool Calling。

### 工作内容

1. 新建 `llm_client.py`，使用 Python 标准库 `urllib` 发送 HTTPS 请求。
2. 从环境变量读取 `LLM_API_KEY`、`LLM_BASE_URL` 和 `LLM_MODEL`，不得记录或打印密钥。
3. 组装 `POST /chat/completions` 请求，传递 `messages`、`tools` 和模型配置。
4. 解析响应中的 `message.content` 和 `message.tool_calls`，转换为 Agent 内部统一格式。
5. 实现网络超时、HTTP 非 2xx、空响应和非法 JSON 的异常类型与错误信息。
6. 在 CLI 中默认使用真实客户端；无 API Key 时给出明确提示，允许通过 `--demo` 使用离线模型。
7. 为成功响应、HTTP 失败和畸形响应编写 mock 测试。

### 验收条件

- 配置有效 API Key 后，CLI 能向 OpenAI 兼容端点发起请求。
- API 失败不会导致未处理异常或泄露密钥。
- API 返回的工具调用可被正确解析为统一数据结构。

## 第三轮：完成多轮工具调用循环

### 目标

将模型决策、本地工具执行和工具结果回传连成可重复的多轮 Agent 循环。

### 工作内容

1. 统一 assistant tool call 的消息格式，保留调用 ID、工具名和原始参数。
2. 解析工具调用参数 JSON；参数为空、格式错误或类型不正确时生成结构化工具错误。
3. 依次执行同一响应中的多个工具调用，并将每个结果追加为 `tool` 消息。
4. 下一轮请求中携带完整的必要消息历史，使模型能够根据工具结果继续决策。
5. 当模型没有工具调用且提供文本回复时结束，并将其作为最终结果。
6. 达到 `max_turns` 后停止，返回明确的终止说明和当前执行摘要。
7. 记录轮次、工具名、参数摘要、成功状态和错误信息，作为可审计执行日志。

### 验收条件

- 使用 mock 模型可验证“读文件 -> 写文件 -> 运行命令 -> 最终回复”的至少三轮流程。
- 工具结果会在下一轮真实发送给模型的消息中出现。
- 无限工具调用会被最大轮数安全终止。

## 第四轮：完善文件工具与工作区安全

### 目标

使本地文件工具具备严格、可预测的边界行为，避免路径穿越和异常文件操作。

### 工作内容

1. 校验工具参数：必填字段、字符串类型、文件路径非空及未知字段处理。
2. 强化 `Workspace.resolve`，覆盖绝对路径、`..`、符号链接逃逸和 Windows 路径差异。
3. `list_files` 支持可选目录与条目数量上限，默认忽略 `.git`、虚拟环境和缓存目录。
4. `read_file` 检查目标是否存在且为普通文本文件，并限制读取大小。
5. `write_file` 支持创建父目录，拒绝目录写入，并返回写入字节数和相对路径。
6. 将所有可恢复错误转换为统一 JSON 结果，而不是让 Agent 主循环崩溃。
7. 为越界、符号链接、二进制文件、缺失文件和嵌套目录写入添加测试。

### 验收条件

- 任意工具均无法读取或写入工作区外文件。
- 常见文件异常以结构化错误回传模型。
- 文件工具测试覆盖正常路径和核心异常路径。

## 第五轮：完善命令执行安全与可观测性

### 目标

将 `run_command` 从基础调用提升为受控执行能力，明确限制危险操作和资源消耗。

### 工作内容

1. 明确命令策略：采用危险命令拒绝规则或可配置白名单，至少阻止删除根目录、磁盘格式化、提权和高风险网络下载等操作。
2. 固定命令工作目录为工作区，不允许通过参数切换执行位置。
3. 保留命令超时，并单独处理超时异常、找不到命令和非零退出码。
4. 截断标准输出和错误输出，标记截断状态，防止上下文被超长输出占满。
5. 在日志中记录命令摘要、退出码、耗时和输出截断状态，不记录敏感环境变量。
6. 为成功命令、失败命令、超时命令、输出截断和危险命令拒绝编写测试。

### 验收条件

- 命令在工作区执行，非零退出和超时以可读结构化结果返回。
- 危险命令不会实际执行。
- 输出不会无限制地进入 Agent 上下文。

## 第六轮：上下文管理与故障恢复

### 目标

在较长任务中保持对话历史可控，并让可恢复失败能够反馈给模型后继续处理。

### 工作内容

1. 为系统提示词定义清晰边界：完成编程任务、优先检查现状、使用工具验证、不得伪造执行结果。
2. 设计消息历史裁剪策略：保留系统消息、用户任务、最近若干轮工具调用和关键结果；对旧输出进行摘要或截断。
3. 区分模型请求失败、工具参数错误、工具执行错误和达到轮数上限等状态。
4. 对单个工具失败，将错误作为 tool result 回传，允许模型选择替代方案。
5. 定义不可恢复错误的最终输出格式，包含失败阶段、原因和已执行操作摘要。
6. 为历史裁剪、连续工具失败、模型暂时失败和达到最大轮数补充测试。

### 验收条件

- 长输出和多轮调用不会无限增长消息历史。
- 工具失败可被模型在后续轮次感知和处理。
- 最终成功、失败和上限停止三类状态均有清晰输出。

## 第七轮：应用服务层与事件模型

### 目标

在 Agent 核心和展示层之间增加稳定的应用服务层，让 CLI、Web 和 GUI 共享任务生命周期、事件和历史数据。

### 工作内容

1. 抽取 `ApplicationService` 或等价模块，提供创建任务、运行任务、取消任务、查询状态和读取历史等接口。
2. 定义统一事件模型，至少覆盖 `task_started`、`model_thinking`、`tool_started`、`tool_finished`、`assistant_message`、`task_finished` 和 `task_error`。
3. 将 Agent 的日志和中间结果转换为事件，不让界面解析内部私有对象。
4. 为任务分配 ID，保存内存中的最近任务；后续可扩展为 SQLite 持久化。
5. 增加取消标记和资源清理接口，避免前端断开后后台任务无期限运行。
6. 让现有 CLI 改为调用服务层，验证核心逻辑没有因增加界面而复制。

### 阶段产物

- `coding_agent/service.py`
- `coding_agent/events.py`
- 可供不同入口调用的任务服务接口
- 一组模拟事件和服务层测试

## 第八轮：Web 后端 API

### 目标

提供本地 Web API，使浏览器前端可以提交任务并观察 Agent 的实时执行过程。

### 工作内容

1. 选择轻量实现方式：优先使用现有 Python 环境中的简单 Web 框架；若不希望增加依赖，则使用标准库 HTTP 服务实现最小 API。
2. 设计 REST 接口，例如创建任务、查询任务、获取历史和列出可用工作区。
3. 增加实时事件传输，优先采用 Server-Sent Events（SSE），必要时再评估 WebSocket。
4. 处理跨域、连接断开、任务不存在、请求参数错误和服务端异常。
5. API 只调用应用服务层，禁止在路由处理器中直接执行任意文件操作或命令。
6. 提供本地启动命令和开发配置，默认只监听 `127.0.0.1`。

### 阶段产物

- `server/` 或 `coding_agent/web/` API 模块
- API schema 或接口说明
- 一个可以被浏览器访问的健康检查接口
- API 层的请求和错误处理测试

## 第九轮：Web 前端最小可用版本

### 目标

建立主要展示界面，让用户可以在浏览器中完成一次完整任务，并看到 Agent 的过程信息。

### 工作内容

1. 选择前端技术栈并初始化独立前端目录；优先采用项目已有工具链，避免引入不必要的 UI 框架。
2. 实现任务输入区、工作区选择或路径输入、开始/停止按钮和连接状态显示。
3. 实现消息时间线：区分用户消息、模型回复、工具调用、工具结果、错误和完成状态。
4. 使用 SSE 或轮询将后端事件实时渲染到页面，处理重连和重复事件。
5. 增加代码/文本查看区域，展示已读取文件、写入文件和命令输出；长内容支持折叠和截断提示。
6. 为加载中、空状态、失败、取消和任务完成设计明确的界面状态。
7. 保持界面只负责展示和发送操作，所有安全校验仍在后端完成。

### 阶段产物

- `frontend/` Web 应用
- 一条从浏览器提交到 Agent 返回最终答复的可运行路径
- 前端启动说明和截图或录屏素材（用于后续展示）

## 第十轮：桌面 GUI 入口

### 目标

增加一个适合本地课堂演示的桌面界面，并复用 Web API 或应用服务层。

### 工作内容

1. 使用 `tkinter` 或项目已有桌面技术实现窗口、任务输入、日志区域和停止按钮。
2. 通过后台线程或异步适配器运行任务，保证界面不会因模型请求或命令执行卡死。
3. 将统一事件模型映射为桌面日志和状态栏；工具调用使用不同颜色或标签区分。
4. 提供“打开工作区”“复制最终结果”“清空会话”等常用操作。
5. 处理窗口关闭时的任务取消和线程回收。
6. 保持 GUI 与 Web 展示的功能边界一致，避免出现只有某个入口支持的核心能力。

### 阶段产物

- `desktop/` 或 `coding_agent/gui.py`
- 本地启动命令
- 至少一次完整任务演示记录

## 第十一轮：命令行体验、日志与使用文档

### 目标

使项目可由其他人在普通开发环境中配置、运行和排查问题。

### 工作内容

1. 完善 CLI 参数，例如 `--workspace`、`--model`、`--max-turns`、`--timeout`、`--demo`、`--verbose`。
2. 交互模式支持连续输入任务和明确退出；单次任务模式返回合适的进程退出码。
3. 增加可选的结构化日志文件，记录每轮模型调用、工具调用和结果摘要。
4. 更新 README：架构图、依赖、环境变量、安装、运行、常见错误、安全边界和已知限制。
5. 检查 `.env.example` 不包含真实密钥，README 不暗示使用被禁止的 Agent 框架或托管工具。

### 验收条件

- 新用户只根据 README 和 `.env.example` 即可启动项目。
- `--verbose` 能帮助定位每轮工具调用，默认模式不过度输出内部细节。
- 文档准确说明安全策略和限制。

## 第十二轮：端到端演示与持续迭代

### 目标

通过真实、可重复的编程任务持续验证 CLI、Web 和 GUI 的完整闭环能力；这一轮不是项目终点，而是后续迭代的基线。

### 工作内容

1. 在 `examples/` 准备一个小型待修复项目或明确的演示任务。
2. 设计任务，使智能体必须完成读取文件、分析问题、修改文件、运行测试和总结结果。
3. 使用真实模型运行一次演示，并保存去敏后的任务、工具调用轨迹、命令输出摘要和最终答复。
4. 运行全量自动化测试，修复发现的问题。
5. 对照 `coding-agent-assignment.md` 的每条要求进行最终检查，记录满足方式和剩余限制。
6. 在 README 中加入演示运行命令及预期结果。
7. 收集界面使用反馈，记录下一轮要优化的交互、性能和安全问题。

### 阶段检查点

- 演示记录清楚展示多轮工具调用闭环。
- 自动化测试全部通过。
- 项目不包含禁止的 Agent 框架、SDK 或服务端代码/文件工具依赖。
- README 足以复现演示，并说明真实模型调用需要用户自行提供 API Key。
- 将遗留问题写入“后续迭代”，不因一次演示而宣称项目最终完成。

## 每轮工作流程

1. 先阅读本轮涉及的现有实现和测试，确认未破坏前序功能。
2. 仅修改完成本轮目标所需的模块，避免无关重构。
3. 增加或更新对应测试，再运行相关测试和全量测试。
4. 更新 README、示例或本计划中的“当前状态”。
5. 记录已完成内容、验证命令、结果和已知限制，然后开始下一轮。
6. 若本轮涉及 UI，补充至少一种可视化验证（截图、录屏或手工操作记录）。
7. 将用户反馈和新需求整理为下一轮任务，保持计划可增量修改。

## 建议的迭代顺序

当前建议按以下顺序推进：

1. 先完成第二至第六轮，稳定 Agent 核心和安全边界。
2. 完成第七轮服务层后，优先实现第八、九轮 Web 入口，形成第一个可展示版本。
3. 再实现第十轮桌面 GUI，补足多形态展示。
4. 最后持续进行第十一、十二轮，根据真实任务和使用反馈迭代。

每次开始新一轮时，先在本计划顶部的“当前状态”中记录上一轮实际完成情况；不要为了追求一次性完整而跳过测试、文档和安全检查。
