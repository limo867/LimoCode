# Coding Agent 当前状态与后续路线

更新时间：2026-08-28

本文档记录当前代码已经实现的能力、尚未完善的部分，以及后续迭代顺序。它是对 `development-plan.md` 的状态化补充，后续每完成一个阶段就更新一次。

## 一、当前实现总览

```text
CLI / Web / tkinter GUI
          |
          v
     AgentService
          |
          v
       Agent
          |
   模型客户端 + 本地工具
          |
          v
       工作区目录
```

当前测试状态：`19` 项自动化测试通过，Git 工作树在最近一次提交后保持干净。

## 二、已实现功能

### 1. Agent 核心

- 维护 system、user、assistant、tool 消息历史。
- 支持模型连续返回工具调用，并执行多轮“模型 -> 工具 -> 结果 -> 模型”循环。
- 支持同一轮多个工具调用。
- 支持最大轮数限制，防止无限循环。
- 记录工具执行摘要和 Agent 最终状态：完成、失败、取消、达到上限。
- 工具参数 JSON 解析失败时，将结构化错误回传模型。

### 2. 模型接入

- 使用 Python 标准库 `urllib` 实现 OpenAI 兼容 Chat Completions 客户端。
- 支持 `LLM_BASE_URL`、`LLM_MODEL`、`LLM_API_KEY` 和请求超时配置。
- 解析文本回复和原生 `tool_calls`。
- 处理 HTTP 错误、网络错误、超时、空响应和非法 JSON。
- 支持临时模型请求失败重试。
- 未配置 API Key 时可使用 `--demo` 离线模式。

### 3. 本地工具

- `list_files`：列出工作区文件，支持目录和数量上限。
- `read_file`：读取 UTF-8 文本文件，限制大小并报告截断。
- `write_file`：创建目录并写入文本文件，限制内容大小。
- `run_command`：固定在工作区执行命令，支持超时、退出码、输出截断和耗时。
- 所有工具统一返回 JSON 风格的成功或错误结果。

### 4. 安全与可靠性

- 文件路径解析限制在配置工作区内。
- 拒绝路径穿越、工作区外绝对路径和符号链接逃逸。
- 校验工具必填参数和未知参数。
- 对删除根目录、格式化、提权、关机等高风险命令进行初步拒绝。
- 限制命令输出、文件内容和上下文历史大小。
- 模型失败、工具失败和达到轮数上限均有明确结果。

### 5. 服务层与事件

- `AgentService` 为 CLI、Web、GUI 提供统一任务创建、查询、取消和事件读取接口。
- 任务在后台线程运行，不阻塞 Web 请求或 GUI 主线程。
- 统一事件包括任务开始、模型处理、工具开始、工具结束、助手回复、任务完成、失败和取消。
- 当前保存最近任务和事件于内存中。

### 6. Web 与 GUI

- 标准库 `ThreadingHTTPServer` 提供本地 Web API。
- 已提供健康检查、创建任务、查询任务、取消任务和 SSE 事件流接口。
- Web 前端支持任务输入、Demo/真实模式选择、实时事件时间线、停止任务和状态显示。
- `tkinter` GUI 支持任务输入、开始/停止、打开工作区和事件日志显示。
- 提供 `examples/demo-task.md` 作为端到端演示任务说明。

## 三、当前限制与风险

这些问题不代表核心结构错误，但会影响真实使用和展示效果：

1. `DemoModel` 已升级为确定性工具脚本，可离线展示列文件、读文件、写文件和运行命令闭环；其产物写入 `.coding-agent-demo/`，仍不代表真实模型的自主规划能力。
2. Web 前端目前主要显示事件 JSON，文件内容查看、差异对比、命令输出折叠和历史任务列表仍较初级。
3. Web API 的任务和事件目前只存内存，服务重启后历史丢失，也没有用户级会话隔离；单进程内已支持任务列表、容量清理和事件序号重连。
4. 取消任务只能设置取消标记；正在阻塞的模型请求或子进程不一定能立即停止。
5. 危险命令识别是规则匹配，不能替代操作系统级沙盒；当前不支持人工确认流程。
6. CLI、Web 和 GUI 的配置入口还未完全统一，例如部分参数只能通过环境变量设置。
7. 尚未用真实 API Key 完成一次去敏的端到端运行记录；当前验证主要使用 mock 模型和 Demo 模式。
8. 暂无正式前端构建、安装打包、跨平台兼容和并发压力测试。

## 四、阶段进度

- 阶段 A（演示闭环）：已完成。`DemoModel` 现在会依次调用 `list_files`、`read_file`、`write_file` 和 `run_command`，可在无 API Key 时展示真实本地工具闭环。
- 阶段 B（Agent 服务契约）：已完成。任务列表、事件序号、按序号恢复 SSE 和任务容量清理已经加入。
- 阶段 C（Web 展示质量）：已完成基础版本。页面已支持任务历史、工具事件摘要、SSE 事件去重和断线重连。

## 五、下一步工作顺序

### 阶段 A：先修正演示闭环

目标：让没有 API Key 的用户也能看到真实的多轮工具调用效果。

工作：

1. 将 `DemoModel` 改为可配置的确定性脚本模型，至少模拟列文件、读文件、写文件和运行测试四步。
2. 在演示工作区加入一个很小的待修复项目和可重复测试。
3. 保存一份去敏后的事件轨迹和最终结果。
4. 修复 Demo 模式下 Web API 的默认行为，确保命令行参数和请求体语义一致。

完成标志：CLI、Web 和 GUI 都能在无 API Key 时完成一次可观察的多轮本地任务。

### 阶段 B：完善 Agent 服务契约

目标：让服务层适合长期支撑多个界面。

工作：

1. 抽象任务状态机，明确 queued、running、completed、failed、cancelled 的合法转换。
2. 为事件增加单调序号，修复 SSE 重连和重复事件处理。
3. 增加任务列表、历史查询、事件分页和最大任务数清理策略。
4. 将取消标记传递到命令执行和模型请求适配层，补充资源回收。
5. 为服务层增加并发任务、断开连接和取消竞态测试。

完成标志：服务层接口文档稳定，Web 和 GUI 不需要读取 Agent 内部字段。

### 阶段 C：提升 Web 展示质量

目标：形成主要的可展示版本。

工作：

1. 增加任务历史侧栏和当前任务详情。
2. 将工具调用显示为名称、参数、状态、耗时和结果摘要，而不是直接展示原始 JSON。
3. 增加文件内容查看、修改前后差异和命令输出折叠。
4. 增加连接重试、事件去重、任务取消确认和错误提示。
5. 优化窄屏布局、空状态、加载状态和完成状态。
6. 用浏览器实际访问页面，保存桌面和移动端截图作为展示材料。

完成标志：用户可以从浏览器提交任务、观察过程、查看修改结果并复制最终总结。

### 阶段 D：完善桌面 GUI

目标：让桌面 GUI 适合课堂演示和本地使用。

工作：

1. 增加模型、工作区、轮数和超时配置面板。
2. 使用后台线程安全地刷新事件，处理窗口关闭和任务取消。
3. 增加文件差异、命令结果和最终答复的独立区域。
4. 增加复制结果、打开文件夹和清空会话操作。
5. 检查 Windows、macOS、Linux 上 tkinter 的启动说明。

完成标志：GUI 与 Web 共享同一服务接口，且核心功能不产生重复实现。

### 阶段 E：真实模型与安全增强

目标：在真实模型任务中提升可控性。

工作：

1. 使用真实 OpenAI 兼容 API 完成一次去敏端到端任务记录。
2. 增加模型请求指数退避、速率限制和更清晰的错误分类。
3. 增加危险命令人工确认或可配置白名单。
4. 评估子进程隔离、资源限制和更严格的工作区权限策略。
5. 增加超长上下文、连续失败和并发任务压力测试。

完成标志：真实模型运行可复现，失败行为可解释，敏感信息不会进入日志或前端。

### 阶段 F：打包、文档与持续迭代

目标：降低其他用户运行和复现成本。

工作：

1. 完善 README、API 说明、配置示例和常见问题。
2. 添加统一启动脚本和可选依赖说明。
3. 增加 CI 或本地一键测试命令。
4. 评估 Web 前端静态资源打包和桌面应用打包方案。
5. 每次迭代使用独立 Git 提交，提交信息说明阶段和验证结果。
6. 记录用户反馈，维护本文件的“当前限制”和“下一步顺序”。

## 六、建议的 Git 维护方式

每个阶段完成后执行：

```powershell
python -m unittest discover -s tests -v
python -m compileall -q coding_agent main.py web_server.py
git status --short
git add .
git commit -m "describe one iteration"
```

提交前确认：没有 API Key、临时日志、缓存文件或真实用户数据进入 Git；提交后保留可回滚的阶段性版本。

## 七、当前推荐起点

下一次迭代优先执行阶段 D，再执行阶段 E。阶段 D 聚焦桌面 GUI 的配置面板、结果查看和跨平台启动；阶段 E 聚焦真实模型运行记录、人工确认和更严格的命令隔离。
