# 编程智能体设计与实现

## 题目要求

个人独立设计并实现一个编程智能体（Coding Agent）。该智能体应能与大语言模型交互，自主地读取和修改本地文件、执行本地命令，并完成用户交给它的编程任务。目标产品可参考 Claude Code、Codex、OpenCode、DeepSeek Harness 等，但实现规模和功能复杂度不限。

不得将现成的 Agent 产品包装为界面，也不得使用任何 Agent 框架或 SDK，包括但不限于：LangChain、LlamaIndex、OpenAI Agents SDK、Claude Agent SDK、AutoGen、CrewAI。

允许使用模型厂商提供的 API 客户端库、OpenAI 兼容网关，以及模型原生的 Tool Calling 接口；但不得依赖 API 服务端托管的代码执行或文件工具，例如 Code Interpreter、Files API。智能体的重要逻辑必须自行实现，包括但不限于：

- 对话历史与上下文管理；
- 工具定义、参数校验与本地执行；
- 模型输出或工具调用结果的解析；
- Agent 循环及终止条件；
- 错误处理、异常恢复与结果反馈。

## 开发 Prompt

你是一名资深软件工程师。请从零实现一个本地运行的编程智能体（Coding Agent），用于根据用户的自然语言任务修改代码仓库并验证结果。

### 实现目标

1. 提供命令行交互入口，用户可以输入编程任务。
2. 调用一个大语言模型 API，并维护完整且可控的多轮消息历史。
3. 为模型提供本地工具，至少包含：
   - `list_files`：列出工作区内的文件；
   - `read_file`：读取指定文本文件；
   - `write_file`：创建或覆盖工作区内的文本文件；
   - `run_command`：在工作区内执行受控命令。
4. 实现“模型决策 -> 工具调用 -> 本地执行 -> 工具结果回传 -> 模型继续决策”的循环，直到模型给出最终答复、达到最大轮数，或出现不可恢复错误。
5. 不使用 LangChain、LlamaIndex、OpenAI Agents SDK、Claude Agent SDK、AutoGen、CrewAI 或任何其他 Agent 框架/SDK。
6. 不使用服务端托管的代码执行、沙盒、文件上传或文件管理能力。文件和命令工具必须由本地程序自行实现。

### 安全与健壮性要求

1. 所有文件操作必须限制在配置的工作区目录内，拒绝路径穿越及工作区外访问。
2. 命令执行应设置超时、输出长度上限和明确的工作目录；对危险命令采取拒绝、确认或白名单策略。
3. 校验工具参数，捕获文件读写、命令执行、模型请求和 JSON 解析异常，并将结构化错误结果返回模型。
4. 限制 Agent 最大调用轮数，避免无限循环；循环结束时明确说明成功、失败或因上限停止。
5. API Key 等敏感信息只从环境变量或本地未提交配置中读取，不得硬编码或输出到日志。

### 建议架构

```text
CLI / 用户任务
      |
      v
Agent 主循环 <----> 对话历史管理
      |
      v
LLM API / Tool Calling
      |
      v
工具调度器
  |       |        |        |
文件列表  文件读写  命令执行  错误处理
      |
      v
本地工作区
```

建议将实现拆分为以下职责明确的模块：

- `main`：解析命令行参数、加载配置、启动交互；
- `agent`：管理消息历史、调用模型、执行循环与终止判断；
- `tools`：声明工具 schema、校验参数、执行本地文件和命令操作；
- `workspace`：解析并校验路径，确保操作不会越出工作区；
- `config`：读取环境变量和运行配置；
- `tests`：覆盖路径限制、工具参数、命令失败、轮数上限等关键场景。

### 交付要求

请输出一个可运行的项目，并至少包含：

- 完整源代码与依赖说明；
- `README.md`：架构说明、安装步骤、环境变量、运行方式、限制与安全策略；
- `.env.example`：只包含变量名和示例，不包含真实密钥；
- 自动化测试或可重复的测试脚本；
- 一个演示任务及其运行记录，展示智能体完成读取代码、修改文件、执行测试或命令、给出总结的完整过程。

### 验收标准

1. 用户给出一个明确的编程任务后，智能体能自主选择并调用本地工具完成任务。
2. 模型可在多轮工具调用后根据工具结果继续推理，而不是只执行一次调用。
3. 所有本地文件和命令操作均由项目代码实现，不依赖 Agent 框架或云端代码/文件工具。
4. 工作区边界、异常处理、最大轮数和命令失败等边界情况有明确行为。
5. 项目可按 README 在一台配置了 API Key 的普通开发环境中复现运行。

### 输出约束

- 优先选择简单、稳定、易读的实现，避免无关的复杂抽象。
- 若模型 API 不支持原生 Tool Calling，请自行约定严格的 JSON 工具调用协议并解析执行。
- 每次工具调用的名称、参数摘要、执行结果和错误信息应保留在对话历史或日志中，便于调试与复盘。
- 最终答复应简要说明完成的修改、执行过的验证命令及其结果、仍存在的限制或失败原因。
